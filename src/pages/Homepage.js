import { Box, Typography } from "@mui/material";

const Homepage = () => {
  return (
    <Box component="main">
      <Typography component="h1" variant="h1">
        Ini ngetes body
      </Typography>
    </Box>
  );
};

export default Homepage;
