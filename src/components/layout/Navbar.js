import { Box, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <Box sx={{ backgroundColor: "#26BAEE" }} px={5} py={3}>
      <Typography component="p">Ini navbar</Typography>
    </Box>
  );
};

export default Navbar;
